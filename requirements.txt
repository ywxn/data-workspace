# AI Data Workspace - Requirements
# Python 3.10+ required

# Core Data Processing
numpy>=1.24.0
openpyxl>=3.1.0
tabulate>=0.9.0

# AI/LLM Providers
openai>=1.0.0
anthropic>=0.7.0
httpx>=0.24.0  # HTTP client for local LLM communication

# GUI Framework
PySide6>=6.6.0

# Database Support
SQLAlchemy>=2.0.0
mysql-connector-python>=8.0.33
psycopg2-binary>=2.9.0
pyodbc>=5.0.0
cx_Oracle>=8.3.0

# Data Visualization
altair>=5.0.0

# Markdown/HTML Processing
markdown>=3.4.0

# Utilities
chardet>=5.1.0
pydantic>=2.0.0
requests>=2.31.0
keyring>=24.0.0

# Type hints and validation
typing-extensions>=4.0.0

# NLP and Embeddings
sentence-transformers>=2.2.0
scipy>=1.10.0

# Self Hosted LLM Server (optional — only needed for built-in model server)
# The application works without these and shows a helpful message if missing.
llama-cpp-python>=0.2.0  # Enables self-hosted model serving
uvicorn>=0.22.0  # ASGI server for running the self-hosted model API
fastapi>=0.104.0  # Web framework for the self-hosted model API
starlette>=0.27.0  # ASGI toolkit for building async web services
sse-starlette>=0.15.0  # Server-Sent Events support for real-time updates
starlette-context>=0.4.0  # Context management for Starlette applications
pydantic-settings>=2.0.0  # Settings management for Pydantic